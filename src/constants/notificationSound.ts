/** Bundled in-app / foreground notification chime. */
export const NOTIFICATION_SOUND_ASSET = require('../../assets/notification_recived_sount.mp3');

/** iOS APNs / Notifee — filename in the app bundle. */
export const NOTIFICATION_SOUND_IOS = 'notification_recived_sount.mp3';

/** Android res/raw resource name (no extension, underscores only). */
export const NOTIFICATION_SOUND_ANDROID = 'notification_recived_sount';
